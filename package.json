{
  "name": "цель-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "Цель PWA — Task tracker + Gamification + Telegram Bot",
  "main": "index.html",
  "scripts": {
    "dev":   "npx serve . -p 3000",
    "build": "echo 'Static — no build needed'",
    "start": "npx serve . -p 3000"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
